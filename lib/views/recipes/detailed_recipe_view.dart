import 'package:flutter/material.dart';

class DetailedRecipeView extends StatelessWidget {
  const DetailedRecipeView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}