import 'package:flutter/material.dart';

class CreateUpdateRecipeView extends StatelessWidget {
  const CreateUpdateRecipeView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}